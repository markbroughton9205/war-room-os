import type { LootOpportunity } from './types'
export type RankingFactor={name:string;contribution:number;explanation:string}; export type LootRanking={score:number;rankable:boolean;vetoReason:string|null;factors:RankingFactor[]}
export function rankLootOpportunity(o:LootOpportunity, now=Date.now()):LootRanking { if(o.expiresAt&&Date.parse(o.expiresAt)<now)return{score:0,rankable:false,vetoReason:'expired',factors:[{name:'expiration',contribution:0,explanation:'Expired opportunities are vetoed.'}]}; const factors:RankingFactor[]=[{name:'confidence',contribution:o.confidence*.4,explanation:'Confidence contributes up to 40.'},{name:'evidence',contribution:o.evidenceLabel==='PROVIDER_CONFIRMED'?25:o.evidenceLabel==='LIVE_SIGNAL_BACKED'?15:0,explanation:'Only live or provider-confirmed evidence contributes.'}]; if(o.estimatedReward?.amount!=null)factors.push({name:'reward',contribution:Math.min(20,o.estimatedReward.amount),explanation:'Known estimated reward contributes up to 20.'}); if(o.estimatedReward?.amount!=null&&o.estimatedTimeMinutes&&o.estimatedTimeMinutes>0)factors.push({name:'hourly_equivalent',contribution:Math.min(15,o.estimatedReward.amount/o.estimatedTimeMinutes*60),explanation:'Known estimate/time contributes up to 15.'}); return{score:Math.round(factors.reduce((s,f)=>s+f.contribution,0)*100)/100,rankable:true,vetoReason:null,factors} }
