const allowed=['research','discover','classify','normalize','rank','calculate','dedupe','flag_deadline'] as const; const forbidden=['submit_survey_answers','submit_receipt','claim_reward','redeem_reward','cash_out','fabricate_evidence','bypass_eligibility','circumvent_antibot','enter_payment_information','sign_contract','accept_legal_terms','send_email_or_message'] as const
export function gateIncomeLootAction(action:string):{allowed:boolean;reason:string}{return (allowed as readonly string[]).includes(action)?{allowed:true,reason:'Internal read-only action.'}:{allowed:false,reason:(forbidden as readonly string[]).includes(action)?'External or prohibited action.':'Unknown actions are denied by default.'}}
