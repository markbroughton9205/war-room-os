'use client'

/**
 * The old cursor trail attached a page-wide mousemove listener plus a full-screen
 * requestAnimationFrame canvas loop. War Room keeps its matrix atmosphere through
 * background layers, but the cursor effect stays disabled to avoid INP pressure.
 */
export function WandTrail() {
  return null
}
