# Minimal Stable Council Mode â€” isolates provider calls from shared orchestration middleware.
# Set to true during emergency stability sprints; false for full council features.
COUNCIL_STABILITY_MODE=false

# Moonshot (Kimi) API — server-only (KIMI_API_KEY preferred; MOONSHOT_API_KEY accepted as fallback)
KIMI_API_KEY=
MOONSHOT_API_KEY=
